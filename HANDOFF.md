# 받은 뒤 할 일 — 서로에게 남기는 말

두 사람 다 Claude 로 작업한다. 이 파일은 `CLAUDE.md` 가 불러오므로, 이 저장소에서
Claude 로 작업을 시작하면 저절로 읽힌다. 규칙은 `AGENTS.md` 6번.

- **Claude 에게**: `git config user.name` 으로 지금 누가 작업하는지 본다(`Kim Min` =
  김민, `금윤호` = 금윤호). 그 사람 앞으로 온 말이 아래에 있으면, 다른 일을 하기 전에
  사용자에게 먼저 알린다. 명령 실행 같은 할 일은 사용자 허락을 받고 한다.
- 처리한 말은 이 파일에서 지우고 커밋한다. 지난 말은 git 이력에 남는다.
- 공개 저장소다. 비밀번호·키·`.env` 값은 절대 적지 않는다.

---

## 금윤호에게 — 2026-09-26 · 김민(Claude)

남겨 준 말(물 칸 삭제 · `prisma generate` · PC 틀 `desk:` · 분석 탭을 홈으로)은 받아서 처리했다.

### 받은 뒤 할 일

1. 개발 서버를 다시 켠다. 운동 목록 캐시 이름을 `library:exercises:v3` 로 바꿨다
   (`lib/library-cache.ts`) — 스크립트로 넣은 운동은 캐시를 안 비워서, 이름을 바꿔야 보인다.

### 알아 두면 좋은 것

- 라이브러리 **암케어에 운동 33개**를 더했다 — 등척성 밀기 · 리바운드 · 드롭 캐치 · 과부하
  내리기 같은 훈련 방식과 TRX Y·T·I 등. 영상은 공개 유튜브 참고 영상이고, 설명·근육·처방까지
  채웠다(`scripts/add-armcare-reference.mts` · `scripts/armcare-reference.json`). DB 구조는 그대로다.
