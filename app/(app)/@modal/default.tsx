/**
 * 팝업 자리(@modal) — 평소에는 비어 있다.
 *
 * 앱 안에서 날짜 화면(/pitch-log/<날짜>)으로 가면 (.)pitch-log 가 이 자리에 팝업을 띄운다
 * (Next 의 '가로채는 경로'). 주소로 곧장 들어오거나 새로고침하면 이 파일이 쓰여 팝업 없이
 * 그 화면만 뜬다.
 */
export default function ModalSlotDefault() {
  return null;
}
